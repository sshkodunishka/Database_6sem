@model lab2_v2.Models.ToursViewModel

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<form method="get">
    <div class="form-inline">
        <label class="control-label">Статус: </label>
        @Html.DropDownList("status", Model.Statuses as SelectList,
                    htmlAttributes: new { @class = "form-control" })

        <input type="submit" value="Фильтр" class="btn btn-default" />
    </div>
</form>

<table class="table">
    <tr>
        <th>Tourname</th>
        <th>descr</th>
        <th>Country</th>
        <th></th>
    </tr>


    @foreach (var item in Model.Tours)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tourName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.descr)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Countries.country)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.idTours }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.idTours })
            </td>
        </tr>
    }

</table>

@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tourName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.descr)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.idCountry)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tourName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.descr)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idCountry)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.idTours }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.idTours })
            </td>
        </tr>
    }

    </table>*@
